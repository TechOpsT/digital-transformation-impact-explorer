# License selection is intentionally deferred until publication intent is confirmed.
# Client-error coverage is enforced by the project-specific domain validator because
# bounded reads and operational endpoints do not all have meaningful 4xx outcomes.
extends:
  - recommended
rules:
  info-license: off
  operation-4xx-response: off
